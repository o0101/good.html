{
  "name": "bang.html",
  "version": "2.0.0",
  "description": "BANG! Web Component custom elements with async templating, smooth syntax and custom void  tags!",
  "main": "dist/bang.js",
  "scripts": {
    "test": "npm run clean && watch -n 3 ./scripts/cpfiles.sh & serve -p 8080 docs/",
    "prod": "export BANG_PROD=test || set BANG_PROD=test&&npm run clean && watch -n 3 ./scripts/cpfiles.sh & serve -p 8080 docs/",
    "clean": "rm -rf dist/ && rm -rf docs/vv && rm docs/*.js || :",
    "prepublishOnly": "export BANG_PROD=test || set BANG_PROD=test&&echo 'npm run pack' is not ready for mobile && rm -rf dist/ && ./scripts/cpfiles.sh",
    "pack": "rm -rf dist/ && mkdir dist/ && cp src/style.css dist/style.css && npx roadroller -t text -a eval src/bang.js -o dist/bang.js && cp dist/bang.js docs/bang-dev.js",
    "tutorial": "npm run test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com//i5ik/bang.git"
  },
  "keywords": [
    "bang!",
    "custom-elements",
    "web-components",
    "async",
    "smooth-syntax",
    "void-tags",
    "self-closing-tags",
    "bang",
    "front-end",
    "framework",
    "javascript",
    "view",
    "dosy",
    "dosyago",
    "dosycorp"
  ],
  "author": "@dosy",
  "license": "MIT",
  "devDependencies": {
    "@babel/plugin-proposal-private-methods": "^7.14.5",
    "@babel/plugin-transform-modules-commonjs": "^7.15.4",
    "@babel/plugin-transform-strict-mode": "^7.14.5",
    "@babel/preset-env": "^7.15.6",
    "babel-core": "^6.26.3",
    "babel-loader": "^8.2.2",
    "babel-preset-env": "^1.7.0",
    "roadroller": "^2.1.0",
    "serve": "^12.0.1",
    "vanillaviewjs": "^1.3.0",
    "webpack": "^5.53.0",
    "webpack-cli": "^4.8.0"
  }
}
